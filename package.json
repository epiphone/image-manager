{
  "name": "image-manager",
  "version": "0.1.0",
  "repository": "https://github.com/epiphone/image-manager",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "all:build": "yarn typecheck && yarn clean && yarn resizer:build && yarn sqiper:build && yarn labeler:build",
    "resizer:build": "parcel build -t node -d src/resizer --no-source-maps src/resizer/handler.ts",
    "resizer:zip": "(cd src/resizer && zip -9 -x *.ts* -r resizer.zip .)",
    "resizer:push": "gsutil cp src/resizer/resizer.zip gs://imgmgr-functions",
    "resizer:deploy": "gcloud beta functions deploy resizer --runtime nodejs10 --trigger-resource imgmgr-images --trigger-event google.storage.object.finalize --region us-east1 --source gs://imgmgr-functions/resizer.zip --service-account serverless@imgmgr.iam.gserviceaccount.com --entry-point resizer --memory 1024MB",
    "resizer:update": "yarn clean && yarn typecheck && yarn resizer:build && yarn resizer:zip && yarn resizer:push && yarn resizer:deploy",
    "sqiper:build": "parcel build -t node -d src/sqiper --no-source-maps src/sqiper/handler.ts",
    "sqiper:zip": "(cd src/sqiper && zip -9 -x *.ts* -r sqiper.zip .)",
    "sqiper:push": "gsutil cp src/sqiper/sqiper.zip gs://imgmgr-functions",
    "sqiper:deploy": "gcloud beta functions deploy sqiper --runtime nodejs10 --trigger-resource imgmgr-images --trigger-event google.storage.object.finalize --region us-east1 --source gs://imgmgr-functions/sqiper.zip --service-account serverless@imgmgr.iam.gserviceaccount.com --entry-point sqiper --memory 1024MB --timeout 120s",
    "sqiper:update": "yarn clean && yarn typecheck && yarn sqiper:build && yarn sqiper:zip && yarn sqiper:push && yarn sqiper:deploy",
    "labeler:build": "parcel build -t node -d src/labeler --no-source-maps src/labeler/handler.ts",
    "labeler:zip": "(cd src/labeler && zip -9 -x *.ts* -r labeler.zip .)",
    "labeler:push": "gsutil cp src/labeler/labeler.zip gs://imgmgr-functions",
    "labeler:deploy": "gcloud beta functions deploy labeler --runtime nodejs10 --trigger-resource imgmgr-images --trigger-event google.storage.object.finalize --region us-east1 --source gs://imgmgr-functions/labeler.zip --service-account serverless@imgmgr.iam.gserviceaccount.com --entry-point labeler --memory 1024MB",
    "labeler:update": "yarn clean && yarn typecheck && yarn labeler:build && yarn labeler:zip && yarn labeler:push && yarn labeler:deploy",
    "server:build": "tsc",
    "server:watch": "tsc -w",
    "server:serve": "nodemon ./dist/app.js",
    "server:start": "node dist/app.js",
    "server:dev": "yarn server:build && npm-run-all -p server:watch server:serve",
    "clean": "rm -f src/**/*.zip",
    "lint": "tslint -p tsconfig.json",
    "sls": "sls",
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc --noEmit"
  },
  "author": "Aleksi Pekkala",
  "license": "MIT",
  "prettier": {
    "printWidth": 100,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/multer": "^1.3.7",
    "@types/node": "^11.13.0",
    "@types/sharp": "^0.22.1",
    "nodemon": "^1.18.11",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.3",
    "prettier": "^1.16.4",
    "tslint": "^5.15.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.4.1"
  },
  "dependencies": {
    "@google-cloud/storage": "^2.4.3",
    "@google-cloud/trace-agent": "^3.6.0",
    "@google-cloud/vision": "^0.25.0",
    "express": "^4.16.4",
    "multer": "^1.4.1",
    "sharp": "^0.22.0",
    "sqip": "^0.3.3"
  }
}
